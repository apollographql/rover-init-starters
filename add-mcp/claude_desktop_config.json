{
  "mcpServers": {
    "mcp-{{PROJECT_NAME}}": {
      "command": "npx",
      "args": [
        "mcp-remote",
        "http://127.0.0.1:5000/mcp"
      ],
      "env": {
        "APOLLO_KEY": "{{APOLLO_KEY}}",
        "APOLLO_GRAPH_REF": "{{APOLLO_GRAPH_REF}}"
      }
    }
  }
}