{
  "mcpServers": {
    "{{PROJECT_NAME}}-graphql": {
      "command": "{{MCP_SERVER_BINARY}}",
      "args": [
        "{{MCP_CONFIG_PATH}}"
      ],
      "env": {
        "APOLLO_KEY": "{{APOLLO_KEY}}",
        "APOLLO_GRAPH_REF": "{{APOLLO_GRAPH_REF}}"
      }
    }
  }
}